language: node_js
node_js: 12.13.0
cache: npm

# install: - npm install
script: - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: V_15_OcenjivanjeKlinika
